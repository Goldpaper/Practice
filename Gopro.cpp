#include <iostream>

using namespace std;

int maisn()
{
    int user;
    int opp;

    vector<int> schema;
    vector<int> opcode;

    cin >> user;
    cin >> opp;
    
    if (user <= 0)
        return -1;

    for(user > 0)
        cout << opp>>1\n;
}